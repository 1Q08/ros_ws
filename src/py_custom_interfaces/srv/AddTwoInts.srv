# ============================================================
# AddTwoInts.srv —— 自定义服务接口定义(srv)
# rosidl 会根据本文件生成 Request / Response 两个类。
# 语法:`---` 上面是请求字段,下面(仅一个 `---`)是响应字段。
# ============================================================

# 请求(Request)字段:客户端发给服务端
int64 a
int64 b
---
# 响应(Response)字段:服务端返回给客户端
int64 sum